Let's build.

---

## Session 1: Foundation

Open **Command Prompt as Administrator**, navigate to your project, and activate the virtual environment:

```bash
cd C:\Users\Blessings\Documents\NeuroSynapse
venv\Scripts\activate
```

You should see `(venv)` at the start of your prompt.

---

### Step 1: Create Django Project

```bash
django-admin startproject core backend
```

This creates the Django project inside the `backend` folder.

---

### Step 2: Create Django Apps

```bash
cd backend
python manage.py startapp monitoring
python manage.py startapp ai_engine
python manage.py startapp healing
python manage.py startapp dashboard
```

---

### Step 3: Create Folder Structure for Other Files

```bash
cd C:\Users\Blessings\Documents\NeuroSynapse
mkdir simulation
mkdir training
mkdir tests
mkdir models
```

---

### Step 4: Create requirements.txt

Create a new file at:
```
C:\Users\Blessings\Documents\NeuroSynapse\requirements.txt
```

Copy and paste this:

```
django>=4.2,<5.0
djangorestframework>=3.14
netmiko>=4.3
paramiko>=3.4
asyncssh>=2.14
scapy>=2.5
scikit-learn>=1.3
numpy>=1.24
pandas>=2.0
pyod>=1.1
joblib>=1.3
python-dotenv>=1.0
bcrypt>=4.1
torch>=2.0
```

---

### Step 5: Create .env File

Create a new file at:
```
C:\Users\Blessings\Documents\NeuroSynapse\backend\.env
```

Copy and paste this:

```
SECRET_KEY=neuro-synapse-dev-key-change-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
```

---

### Step 6: Configure settings.py

Open this file:
```
C:\Users\Blessings\Documents\NeuroSynapse\backend\core\settings.py
```

**Delete everything in it** and paste this:

```python
import os
from pathlib import Path
from dotenv import load_dotenv

load_dotenv()

BASE_DIR = Path(__file__).resolve().parent.parent

SECRET_KEY = os.getenv('SECRET_KEY', 'fallback-secret-key')

DEBUG = os.getenv('DEBUG', 'True') == 'True'

ALLOWED_HOSTS = os.getenv('ALLOWED_HOSTS', 'localhost').split(',')

INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'rest_framework',
    'monitoring',
    'ai_engine',
    'healing',
    'dashboard',
]

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
]

ROOT_URLCONF = 'core.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [BASE_DIR / 'templates'],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]

WSGI_APPLICATION = 'core.wsgi.application'

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': BASE_DIR / 'db.sqlite3',
    }
}

AUTH_PASSWORD_VALIDATORS = []

LANGUAGE_CODE = 'en-us'
TIME_ZONE = 'Africa/Lusaka'
USE_I18N = True
USE_TZ = True

STATIC_URL = 'static/'
STATICFILES_DIRS = [BASE_DIR / 'static']

DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'
```

---

### Step 7: Create Database Models

Open this file:
```
C:\Users\Blessings\Documents\NeuroSynapse\backend\monitoring\models.py
```

**Delete everything** and paste this:

```python
from django.db import models


class Device(models.Model):
    """Represents a managed network device (router, switch, etc.)"""
    name = models.CharField(max_length=100)
    ip_address = models.GenericIPAddressField(unique=True)
    device_type = models.CharField(max_length=50, default='router')
    vendor = models.CharField(max_length=50, blank=True)
    status = models.CharField(
        max_length=20,
        choices=[
            ('ONLINE', 'Online'),
            ('OFFLINE', 'Offline'),
            ('DEGRADED', 'Degraded'),
        ],
        default='ONLINE'
    )
    ssh_port = models.IntegerField(default=22)
    username = models.CharField(max_length=100, default='admin')
    password = models.CharField(max_length=255, default='password')
    created_at = models.DateTimeField(auto_now_add=True)

    def __str__(self):
        return f"{self.name} ({self.ip_address})"


class Telemetry(models.Model):
    """Stores telemetry data collected from network devices"""
    device = models.ForeignKey(Device, on_delete=models.CASCADE, related_name='telemetry')
    timestamp = models.DateTimeField(auto_now_add=True)
    cpu_usage = models.FloatField(null=True, blank=True)
    memory_usage = models.FloatField(null=True, blank=True)
    latency_ms = models.FloatField(null=True, blank=True)
    packet_loss = models.FloatField(null=True, blank=True)
    bandwidth_util = models.FloatField(null=True, blank=True)
    interface_status = models.JSONField(null=True, blank=True)
    raw_output = models.TextField(blank=True)

    class Meta:
        ordering = ['-timestamp']
        indexes = [
            models.Index(fields=['-timestamp']),
            models.Index(fields=['device', '-timestamp']),
        ]

    def __str__(self):
        return f"Telemetry from {self.device.name} at {self.timestamp}"


class Incident(models.Model):
    """Records detected network anomalies/failures"""
    FAILURE_TYPES = [
        ('SERVICE_CRASH', 'Service Crash'),
        ('LINK_FAILURE', 'Link Failure'),
        ('DDOS_ATTACK', 'DDoS Attack'),
        ('PERFORMANCE_DEGRADATION', 'Performance Degradation'),
        ('UNKNOWN', 'Unknown'),
    ]

    STATUS_CHOICES = [
        ('DETECTED', 'Detected'),
        ('DIAGNOSING', 'Diagnosing'),
        ('READY_FOR_ACTION', 'Ready for Action'),
        ('MANUAL_REVIEW', 'Manual Review'),
        ('HEALING', 'Healing'),
        ('HEALED', 'Healed'),
        ('FAILED', 'Failed'),
        ('ROLLED_BACK', 'Rolled Back'),
    ]

    device = models.ForeignKey(Device, on_delete=models.CASCADE, related_name='incidents')
    telemetry_snapshot = models.ForeignKey(Telemetry, on_delete=models.SET_NULL, null=True)
    failure_type = models.CharField(max_length=50, choices=FAILURE_TYPES, default='UNKNOWN')
    confidence_score = models.FloatField(default=0.0)
    status = models.CharField(max_length=20, choices=STATUS_CHOICES, default='DETECTED')
    description = models.TextField(blank=True)
    detected_at = models.DateTimeField(auto_now_add=True)
    resolved_at = models.DateTimeField(null=True, blank=True)

    class Meta:
        ordering = ['-detected_at']

    def __str__(self):
        return f"{self.failure_type} on {self.device.name} ({self.confidence_score:.2f})"


class HealingAction(models.Model):
    """Records healing actions taken to resolve incidents"""
    ACTION_TYPES = [
        ('RESTART_SERVICE', 'Restart Service'),
        ('REROUTE_TRAFFIC', 'Reroute Traffic'),
        ('BLOCK_SOURCE_IP', 'Block Source IP'),
        ('REBOOT_DEVICE', 'Reboot Device'),
        ('CLEAR_CACHE', 'Clear Cache'),
        ('RATE_LIMIT', 'Rate Limit'),
        ('CUSTOM', 'Custom'),
    ]

    STATUS_CHOICES = [
        ('PENDING', 'Pending'),
        ('VALIDATING', 'Validating in Sandbox'),
        ('EXECUTING', 'Executing'),
        ('EXECUTED', 'Executed'),
        ('FAILED', 'Failed'),
        ('ROLLED_BACK', 'Rolled Back'),
    ]

    incident = models.ForeignKey(Incident, on_delete=models.CASCADE, related_name='healing_actions')
    action_type = models.CharField(max_length=30, choices=ACTION_TYPES)
    command = models.TextField()
    status = models.CharField(max_length=20, choices=STATUS_CHOICES, default='PENDING')
    sandbox_result = models.CharField(max_length=20, blank=True)
    execution_output = models.TextField(blank=True)
    created_at = models.DateTimeField(auto_now_add=True)
    completed_at = models.DateTimeField(null=True, blank=True)

    class Meta:
        ordering = ['-created_at']

    def __str__(self):
        return f"{self.action_type} for {self.incident}"


class RLReward(models.Model):
    """Stores reinforcement learning rewards for healing actions"""
    healing_action = models.OneToOneField(HealingAction, on_delete=models.CASCADE, related_name='reward')
    reward_value = models.IntegerField(choices=[(1, 'Success'), (-1, 'Failure')])
    state_context = models.JSONField(blank=True)
    created_at = models.DateTimeField(auto_now_add=True)

    def __str__(self):
        return f"Reward {self.reward_value} for {self.healing_action}"


class RLPolicy(models.Model):
    """Stores the RL agent's learned policy"""
    policy_name = models.CharField(max_length=100, unique=True)
    policy_data = models.JSONField()
    performance_score = models.FloatField(default=0.0)
    training_episodes = models.IntegerField(default=0)
    updated_at = models.DateTimeField(auto_now=True)

    def __str__(self):
        return f"Policy {self.policy_name} (Score: {self.performance_score})"
```

---

### Step 8: Run Initial Migrations

```bash
python manage.py makemigrations
python manage.py migrate
```

You should see output showing tables being created.

---

### Step 9: Create the Mock SSH Server

Create this file:
```
C:\Users\Blessings\Documents\NeuroSynapse\simulation\mock_ssh_server.py
```

Copy and paste this complete code:

```python
"""
NeuroSynapse Mock SSH Server
Pretends to be a Cisco/MikroTik router for development and testing.
Run this in a separate terminal window.
"""

import asyncssh
import asyncio
import random
import os


class MockRouterSSHServer(asyncssh.SSHServer):
    """Pretends to be a managed network device (router/switch)"""
    
    def __init__(self):
        self._conn = None
        self.device_state = self._get_default_state()
    
    def _get_default_state(self):
        return {
            'cpu_usage': 25.0,
            'memory_usage': 40.0,
            'interface_status': {
                'GigabitEthernet0/1': {'status': 'up', 'speed': '1000Mbps', 'errors': 0},
                'GigabitEthernet0/2': {'status': 'up', 'speed': '1000Mbps', 'errors': 0},
                'GigabitEthernet0/3': {'status': 'down', 'speed': 'auto', 'errors': 0},
            },
            'ospf_neighbors': [
                {'neighbor_id': '10.0.0.2', 'state': 'FULL', 'interface': 'Gig0/1'},
                {'neighbor_id': '10.0.0.3', 'state': 'FULL', 'interface': 'Gig0/2'},
            ],
            'bgp_peers': [
                {'peer': '192.168.1.1', 'state': 'Established', 'prefixes': 42},
            ],
            'health_status': 'HEALTHY',
            'active_incident': None,
        }
    
    def connection_made(self, conn):
        self._conn = conn
        peername = conn.get_extra_info('peername')
        print(f"[MOCK ROUTER] Connection from {peername}")
    
    def connection_lost(self, exc):
        print(f"[MOCK ROUTER] Connection closed")
    
    def begin_auth(self, username):
        return True
    
    def password_auth(self, username, password):
        print(f"[MOCK ROUTER] Auth: {username}")
        return True
    
    def session_requested(self):
        return MockRouterSession(self)


class MockRouterSession(asyncssh.SSHServerSession):
    
    def __init__(self, server):
        self.server = server
        self._chan = None
        self._in_config_mode = False
    
    def connection_made(self, chan):
        self._chan = chan
        chan.write("\r\nMockRouterOS v7.0 (stable) - NeuroSynapse Test Environment\r\n")
        self._write_prompt()
    
    def _write_prompt(self):
        if self._in_config_mode:
            self._chan.write("\r\nMockRouter(config)# ")
        else:
            self._chan.write("\r\n[admin@MockRouter] > ")
    
    def data_received(self, data, datatype):
        command = data.strip()
        if not command:
            self._write_prompt()
            return
        
        print(f"[MOCK ROUTER] CMD: '{command}'")
        response = self.process_command(command)
        self._chan.write(response)
        self._write_prompt()
    
    def process_command(self, command):
        cmd_lower = command.lower().strip()
        
        if cmd_lower == 'exit':
            if self._in_config_mode:
                self._in_config_mode = False
                return "Exiting configuration mode.\r\n"
            return "Connection closed.\r\n"
        
        if cmd_lower == 'configure terminal':
            self._in_config_mode = True
            return "Entering configuration mode...\r\n"
        
        if cmd_lower == 'end':
            self._in_config_mode = False
            return "Returning to exec mode.\r\n"
        
        # --- TELEMETRY COMMANDS ---
        if cmd_lower in ['show cpu', 'system resource print', 'show processes cpu']:
            return self._cpu_output()
        
        if cmd_lower in ['show memory', 'show processes memory']:
            return self._memory_output()
        
        if cmd_lower in ['show interface', 'show interfaces', 'interface print']:
            return self._interface_output()
        
        if cmd_lower in ['show ip ospf neighbor', 'ospf neighbor print']:
            return self._ospf_output()
        
        if cmd_lower in ['show ip bgp summary', 'bgp peer print']:
            return self._bgp_output()
        
        # --- HEALING COMMANDS ---
        if 'restart' in cmd_lower and 'service' in cmd_lower:
            return self._restart_service(command)
        
        if 'shutdown' in cmd_lower and 'interface' in cmd_lower:
            return self._shutdown_interface(command)
        
        if 'no shutdown' in cmd_lower or 'no shut' in cmd_lower:
            return self._enable_interface(command)
        
        if 'reboot' in cmd_lower:
            return self._reboot()
        
        # --- FAILURE INJECTION ---
        if cmd_lower == 'inject_service_crash':
            self.server.device_state['cpu_usage'] = 98.0
            self.server.device_state['memory_usage'] = 88.0
            self.server.device_state['health_status'] = 'CRITICAL'
            self.server.device_state['active_incident'] = 'SERVICE_CRASH'
            return "INJECTED: SERVICE_CRASH (CPU: 98%, Memory: 88%)"
        
        if cmd_lower == 'inject_link_failure':
            self.server.device_state['interface_status']['GigabitEthernet0/1']['status'] = 'down'
            self.server.device_state['interface_status']['GigabitEthernet0/1']['errors'] = 999
            self.server.device_state['health_status'] = 'CRITICAL'
            self.server.device_state['active_incident'] = 'LINK_FAILURE'
            return "INJECTED: LINK_FAILURE (Gig0/1 down, 999 errors)"
        
        if cmd_lower == 'inject_ddos':
            self.server.device_state['cpu_usage'] = 99.0
            self.server.device_state['health_status'] = 'CRITICAL'
            self.server.device_state['active_incident'] = 'DDOS_ATTACK'
            return "INJECTED: DDOS_ATTACK (Traffic flood, CPU: 99%)"
        
        if cmd_lower == 'reset_network':
            self.server.device_state = self.server._get_default_state()
            return "NETWORK RESET TO HEALTHY STATE"
        
        return f"% Invalid input: '{command}'\r\n"
    
    def _cpu_output(self):
        cpu = self.server.device_state['cpu_usage']
        if self.server.device_state['health_status'] != 'CRITICAL':
            cpu = max(5, min(45, cpu + random.uniform(-8, 12)))
        return f"""
CPU utilization for five seconds: {cpu:.1f}%
CPU utilization for one minute: {cpu-3:.1f}%
CPU utilization for five minutes: {cpu-6:.1f}%
 PID  Runtime(ms)  Invoked   uSecs   5Sec   1Min   5Min TTY Process
   1        1234     5000     246   0.00%  0.00%  0.00%   0 Chunk Manager
  42       89000    12000    7416   {cpu*0.4:.2f}% {cpu*0.35:.2f}% {cpu*0.3:.2f}%   0 IP Input
"""
    
    def _memory_output(self):
        total = 4096
        used_pct = self.server.device_state['memory_usage']
        if self.server.device_state['health_status'] != 'CRITICAL':
            used_pct = max(20, min(60, used_pct + random.uniform(-8, 12)))
        used = total * used_pct / 100
        free = total - used
        return f"""
                Head    Total(MB)   Used(MB)   Free(MB)  Lowest(MB) Largest(MB)
Processor   0x1F0     {total}       {used:.0f}       {free:.0f}       2800       2800
      I/O   0x1F0     {total}       {used*0.3:.0f}        {free*0.7:.0f}       2800       2800
"""
    
    def _interface_output(self):
        lines = ["Interface              IP-Address      OK? Method Status                Protocol"]
        for name, details in self.server.device_state['interface_status'].items():
            status = details['status']
            proto = 'up' if status == 'up' else 'down'
            lines.append(f"{name:22} 192.168.1.1     YES manual {status:8}            {proto}")
        return "\r\n".join(lines)
    
    def _ospf_output(self):
        lines = ["Neighbor ID     Pri   State           Dead Time   Address         Interface"]
        for n in self.server.device_state['ospf_neighbors']:
            lines.append(f"{n['neighbor_id']:15} 1    {n['state']:15} 00:00:35    192.168.1.2     {n['interface']}")
        return "\r\n".join(lines)
    
    def _bgp_output(self):
        lines = ["BGP router identifier 192.168.1.1, local AS number 65000"]
        lines.append("Neighbor        V    AS MsgRcvd MsgSent   TblVer  InQ OutQ Up/Down  State/PfxRcd")
        for p in self.server.device_state['bgp_peers']:
            lines.append(f"{p['peer']:15} 4 65001   12345   12345        1    0    0 2d03h    {p['prefixes']}")
        return "\r\n".join(lines)
    
    def _restart_service(self, command):
        print(f"[MOCK ROUTER] HEALING: restart_service")
        self.server.device_state['cpu_usage'] = 35.0
        self.server.device_state['memory_usage'] = 42.0
        self.server.device_state['health_status'] = 'HEALTHY'
        self.server.device_state['active_incident'] = None
        return "Service restarted successfully.\r\nProcess nginx started with PID 12345"
    
    def _shutdown_interface(self, command):
        for name in self.server.device_state['interface_status']:
            if name.lower() in command.lower():
                self.server.device_state['interface_status'][name]['status'] = 'administratively down'
                return f"Interface {name} shutdown"
        return "% Interface not found"
    
    def _enable_interface(self, command):
        for name in self.server.device_state['interface_status']:
            if name.lower() in command.lower():
                self.server.device_state['interface_status'][name]['status'] = 'up'
                self.server.device_state['interface_status'][name]['errors'] = 0
                return f"Interface {name} enabled"
        return "% Interface not found"
    
    def _reboot(self):
        self.server.device_state = self.server._get_default_state()
        self.server.device_state['cpu_usage'] = 30.0
        return "Device rebooted successfully.\r\nSystem uptime is 0 days, 0 hours, 1 minute"
    
    def eof_received(self):
        return True
    
    def connection_lost(self, exc):
        pass


def generate_ssh_key():
    """Generate SSH host key if it doesn't exist"""
    key_path = os.path.join(os.path.dirname(__file__), 'ssh_host_key')
    if not os.path.exists(key_path):
        print("[MOCK ROUTER] Generating SSH host key...")
        os.system(f'ssh-keygen -t rsa -f {key_path} -N "" -q')
        print("[MOCK ROUTER] SSH host key generated")
    return key_path


async def start_server():
    key_path = generate_ssh_key()
    
    print("=" * 55)
    print("  NEUROSYNAPSE MOCK ROUTER SSH SERVER")
    print("=" * 55)
    print(f"  Listening: 0.0.0.0:2222")
    print(f"  Username:  admin")
    print(f"  Password:  anything")
    print(f"  Host Key:  {key_path}")
    print("-" * 55)
    print("  Special commands for testing:")
    print("    inject_service_crash  - Simulate service failure")
    print("    inject_link_failure   - Simulate link failure")
    print("    inject_ddos           - Simulate DDoS attack")
    print("    reset_network         - Return to healthy state")
    print("=" * 55)
    print("")
    
    await asyncssh.create_server(
        MockRouterSSHServer,
        '',
        2222,
        server_host_keys=[key_path],
    )
    
    await asyncio.Event().wait()


if __name__ == '__main__':
    try:
        asyncio.run(start_server())
    except KeyboardInterrupt:
        print("\n\n[MOCK ROUTER] Shutting down...")
```

---

### Step 10: Create a Templates Folder

```bash
mkdir C:\Users\Blessings\Documents\NeuroSynapse\backend\templates
mkdir C:\Users\Blessings\Documents\NeuroSynapse\backend\static
```

---

### Step 11: Create Initial URL Configuration

Open:
```
C:\Users\Blessings\Documents\NeuroSynapse\backend\core\urls.py
```

Replace with:

```python
from django.contrib import admin
from django.urls import path, include

urlpatterns = [
    path('admin/', admin.site.urls),
    path('api/', include('monitoring.urls')),
]
```

---

### Step 12: Create Simple Monitoring View

Open:
```
C:\Users\Blessings\Documents\NeuroSynapse\backend\monitoring\views.py
```

Replace with:

```python
from django.http import JsonResponse
from django.views.decorators.csrf import csrf_exempt
import json


def health_check(request):
    """Simple endpoint to verify the API is running"""
    return JsonResponse({
        'status': 'healthy',
        'system': 'NeuroSynapse API',
        'version': '0.1.0',
    })


@csrf_exempt
def telemetry_ingest(request):
    """Endpoint to receive telemetry data"""
    if request.method == 'POST':
        data = json.loads(request.body)
        return JsonResponse({
            'status': 'received',
            'records': len(data) if isinstance(data, list) else 1,
        })
    return JsonResponse({'error': 'POST required'}, status=405)
```

Create:
```
C:\Users\Blessings\Documents\NeuroSynapse\backend\monitoring\urls.py
```

Paste:

```python
from django.urls import path
from . import views

urlpatterns = [
    path('health/', views.health_check, name='health-check'),
    path('telemetry/', views.telemetry_ingest, name='telemetry-ingest'),
]
```

---

### Step 13: Test Django Server

```bash
python manage.py runserver
```

Open your browser to: `http://localhost:8000/api/health/`

You should see:
```json
{"status": "healthy", "system": "NeuroSynapse API", "version": "0.1.0"}
```

**Press Ctrl+C** to stop the server.

---

### Step 14: Test Mock SSH Server

Open a **NEW terminal**:

```bash
cd C:\Users\Blessings\Documents\NeuroSynapse\simulation
python mock_ssh_server.py
```

You should see:
```
=======================================================
  NEUROSYNAPSE MOCK ROUTER SSH SERVER
=======================================================
  Listening: 0.0.0.0:2222
...
```

Open a **THIRD terminal** and test the connection:

```bash
ssh admin@localhost -p 2222
```

Type `yes` for the fingerprint prompt.
Password: `anything`

You should see the router prompt: `[admin@MockRouter] >`

Type: `show cpu`

You should see CPU output.

Type: `exit`

---

### Session 1 Complete

You now have:

| Component | Status | Test |
|-----------|--------|------|
| Django Backend | ✅ Running | Browser shows health check |
| SQLite Database | ✅ Created | Tables migrated |
| Mock SSH Server | ✅ Running | SSH connection works |
| Database Models | ✅ Created | 6 tables ready |

---

**Now do this:**

1. **Update your Build Plan document** - Add today's date and list what we created
2. **Create first backup:**
   ```bash
   xcopy C:\Users\Blessings\Documents\NeuroSynapse C:\Users\Blessings\Documents\NeuroSynapse_Backups\Week_01_Apr30 /E /I
   ```

Tell me **"Session 1 done"** and I'll give you Session 2: the SSH client that connects Django to the mock server, and the telemetry collection loop.